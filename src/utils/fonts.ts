import { Inter } from 'next/font/google';
import { DM_Mono } from 'next/font/google';

const inter = Inter({ subsets: ['latin'], weight: ['700', '900'] });
const dm_mono = DM_Mono({ subsets: ['latin'], weight: '400' });

export { inter, dm_mono };
